package com.swtp4.backend.security;

// Roles for both user groups to check whether a user is authorized to request certain information from the backend api.
public enum Role {
    OFFICE, COMMITTEE
}
